; Schema retrieval functions---given a story, 
; find some candidate schemas to try matching on.

(defun retrieve_candidates (story schemas)
	; Our main strategy is going to be to extract
	; verbs from the story and match them to schemas.
	; In the case of highly general protoschemas
	; with *kinds* of actions, but no specific verbs,
	; 
)
